{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "iac-config-rule-hit.schema.json",
  "title": "One declared-configuration rule hit",
  "description": "Claim class declared_configuration_evidence: an evidence lead about what a file declares.",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "hit_id",
    "assertion",
    "tool_id",
    "rule",
    "category",
    "exposure",
    "resource",
    "location",
    "citation"
  ],
  "properties": {
    "hit_id": {
      "type": "string",
      "pattern": "^IC-[0-9]{6}\\Z",
      "description": "Ordinal identity: IC-000001..n in document order."
    },
    "assertion": {
      "type": "string",
      "enum": [
        "declared-configuration-rule-hit",
        "declared-exposure-lead"
      ]
    },
    "tool_id": {
      "type": "string",
      "pattern": "^[0-9a-z][0-9a-z._-]{0,95}\\Z",
      "description": "Must resolve to a tool instance with a validated output in outputs/tool-results.json."
    },
    "rule": {
      "type": "object",
      "additionalProperties": false,
      "required": [
        "rule_id",
        "rule_pack"
      ],
      "properties": {
        "rule_id": {
          "type": "string",
          "pattern": "^(?!.*[0-9A-Fa-f]{32})[0-9A-Za-z][0-9A-Za-z._-]{0,63}\\Z",
          "description": "Check identity inside the rule pack, e.g. a CKV_, AVD- or DL id."
        },
        "rule_pack": {
          "description": "Must equal one data_identities entry (kind, identity_id, sha256) of this hit's tool instance in tool-results.json. A rule id means nothing without the bundle it came from.",
          "type": "object",
          "additionalProperties": false,
          "required": [
            "kind",
            "identity_id",
            "sha256"
          ],
          "properties": {
            "kind": {
              "type": "string",
              "enum": [
                "rule-pack",
                "policy-bundle"
              ]
            },
            "identity_id": {
              "type": "string",
              "pattern": "^[0-9a-z][0-9a-z._-]{0,95}\\Z"
            },
            "sha256": {
              "type": "string",
              "pattern": "^sha256:[0-9a-f]{64}\\Z"
            }
          }
        }
      }
    },
    "category": {
      "type": "string",
      "enum": [
        "network-exposure",
        "public-access-grant",
        "access-control",
        "encryption-at-rest",
        "encryption-in-transit",
        "logging-monitoring",
        "privilege-escalation",
        "workload-isolation",
        "resource-limits",
        "image-provenance",
        "embedded-credential-material",
        "backup-recovery",
        "other"
      ]
    },
    "exposure": {
      "type": [
        "string",
        "null"
      ],
      "enum": [
        "DECLARED_EXPOSURE",
        "OBSERVED_EXPOSURE",
        null
      ],
      "description": "Non-null exactly when assertion is declared-exposure-lead. OBSERVED_EXPOSURE is schema-valid so the contract validator can reject it by name; this producer may only emit DECLARED_EXPOSURE (ADR-0008 Decision 7, ADR-0010 claim limits)."
    },
    "resource": {
      "type": "object",
      "additionalProperties": false,
      "required": [
        "iac_kind",
        "address",
        "address_disposition"
      ],
      "properties": {
        "iac_kind": {
          "type": "string",
          "enum": [
            "terraform",
            "cloudformation",
            "kubernetes",
            "helm",
            "dockerfile"
          ]
        },
        "address": {
          "type": [
            "string",
            "null"
          ],
          "pattern": "^(?!.*[0-9A-Fa-f]{32})[A-Za-z][A-Za-z0-9_.\\-/]{0,127}\\Z",
          "description": "Declared resource address with index and for_each keys removed, e.g. module.net.aws_security_group.web or Deployment/default/web. Null unless address_disposition is published."
        },
        "address_disposition": {
          "type": "string",
          "enum": [
            "published",
            "withheld-unsafe-address",
            "not-applicable"
          ]
        }
      }
    },
    "location": {
      "description": "Where the rule fired. A location, never content: there is no column, offset, length, text or hash field.",
      "type": "object",
      "additionalProperties": false,
      "required": [
        "path",
        "path_disposition",
        "start_line",
        "end_line"
      ],
      "properties": {
        "path": {
          "type": [
            "string",
            "null"
          ],
          "pattern": "^(?!.*[0-9A-Fa-f]{32})[A-Za-z0-9._+@%~,-][A-Za-z0-9._+@%~,/-]{0,1023}\\Z",
          "description": "Repository-relative normalized POSIX path taken from the snapshot listing (never from tool output), percent-encoded outside the pattern. A path holding a run of 32 or more hex characters is not publishable (a digest-shaped segment could be a per-value hash) and is withheld. Null exactly when path_disposition is withheld-unsafe-path."
        },
        "path_disposition": {
          "type": "string",
          "enum": [
            "published",
            "withheld-unsafe-path"
          ],
          "description": "withheld-unsafe-path: the path itself did not pass the redactor, so the record is kept and the path is not published."
        },
        "start_line": {
          "type": [
            "integer",
            "null"
          ],
          "description": "1-based. Null together with end_line for a whole-file fact."
        },
        "end_line": {
          "type": [
            "integer",
            "null"
          ]
        }
      }
    },
    "citation": {
      "description": "The tool-instance output this record was normalized from. The field set follows threat-model-citation.schema.json without its free-text members (note, line_range, index_record_id). tool-results.json must list this path for this record's tool with the same attempt_id and sha256.",
      "type": "object",
      "additionalProperties": false,
      "required": [
        "source_class",
        "producer",
        "attempt_id",
        "path",
        "sha256"
      ],
      "properties": {
        "source_class": {
          "const": "raw"
        },
        "producer": {
          "type": "string",
          "pattern": "^[0-9a-z][0-9a-z-]{0,95}\\Z",
          "description": "Producing job id; must equal this document's job_id."
        },
        "attempt_id": {
          "type": "string",
          "pattern": "^[0-9A-Za-z][0-9A-Za-z._-]{0,95}\\Z",
          "description": "The TOOL INSTANCE attempt id."
        },
        "path": {
          "type": "string",
          "pattern": "^[A-Za-z0-9._+@%~,-][A-Za-z0-9._+@%~,/-]{0,1023}\\Z"
        },
        "sha256": {
          "type": "string",
          "pattern": "^[0-9a-f]{64}\\Z",
          "description": "Hash of the cited output FILE (bare hex, as in threat-model-citation). Never a hash of a matched value."
        }
      }
    }
  }
}
