{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "secrets-inventory-entry.schema.json",
  "title": "One secrets-inventory location fingerprint",
  "description": "One detector hit as a location. Claim class secret_exposure_lead: an evidence lead, never a finding, a severity or a statement that the credential is live.",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "entry_id",
    "assertion",
    "tool_id",
    "rule_id",
    "data_class",
    "confidence",
    "location",
    "citation"
  ],
  "properties": {
    "entry_id": {
      "type": "string",
      "pattern": "^SI-[0-9]{6}\\Z",
      "description": "Ordinal identity: entries are numbered SI-000001..n in document order, so the id carries nothing but position."
    },
    "assertion": {
      "type": "string",
      "enum": [
        "candidate-secret-location",
        "credential-store-file-present",
        "private-key-header-present"
      ]
    },
    "tool_id": {
      "type": "string",
      "pattern": "^[0-9a-z][0-9a-z._-]{0,95}\\Z",
      "description": "Must resolve to a tool instance with a validated output in outputs/tool-results.json."
    },
    "rule_id": {
      "type": "string",
      "pattern": "^(?!.*[0-9A-Fa-f]{32})[0-9a-z][0-9a-z._-]{0,47}\\Z",
      "description": "Detector rule identity from the pinned rule pack baked into the tool image, never from a target-supplied configuration."
    },
    "data_class": {
      "type": "string",
      "enum": [
        "api-key",
        "access-token",
        "cloud-provider-credential",
        "connection-string",
        "generic-high-entropy",
        "key-store-file",
        "password",
        "private-key",
        "other"
      ],
      "description": "Closed data-class label."
    },
    "confidence": {
      "type": "string",
      "enum": [
        "high",
        "medium",
        "low"
      ],
      "description": "Detector-reported confidence that the location holds a credential. Not a severity."
    },
    "location": {
      "description": "Where the detector fired. A location, never content: there is no column, offset, length, text or hash field.",
      "type": "object",
      "additionalProperties": false,
      "required": [
        "path",
        "path_disposition",
        "start_line",
        "end_line"
      ],
      "properties": {
        "path": {
          "type": [
            "string",
            "null"
          ],
          "pattern": "^(?!.*[0-9A-Fa-f]{32})[A-Za-z0-9._+@%~,-][A-Za-z0-9._+@%~,/-]{0,1023}\\Z",
          "description": "Repository-relative normalized POSIX path taken from the snapshot listing (never from tool output), percent-encoded outside the pattern. A path holding a run of 32 or more hex characters is not publishable (a digest-shaped segment could be a per-value hash) and is withheld. Null exactly when path_disposition is withheld-unsafe-path."
        },
        "path_disposition": {
          "type": "string",
          "enum": [
            "published",
            "withheld-unsafe-path"
          ],
          "description": "withheld-unsafe-path: the path itself did not pass the redactor, so the record is kept and the path is not published."
        },
        "start_line": {
          "type": [
            "integer",
            "null"
          ],
          "description": "1-based. Null together with end_line for a whole-file fact."
        },
        "end_line": {
          "type": [
            "integer",
            "null"
          ]
        }
      }
    },
    "citation": {
      "description": "The tool-instance output this record was normalized from. The field set follows threat-model-citation.schema.json without its free-text members (note, line_range, index_record_id). tool-results.json must list this path for this record's tool with the same attempt_id and sha256.",
      "type": "object",
      "additionalProperties": false,
      "required": [
        "source_class",
        "producer",
        "attempt_id",
        "path",
        "sha256"
      ],
      "properties": {
        "source_class": {
          "const": "raw"
        },
        "producer": {
          "type": "string",
          "pattern": "^[0-9a-z][0-9a-z-]{0,95}\\Z",
          "description": "Producing job id; must equal this document's job_id."
        },
        "attempt_id": {
          "type": "string",
          "pattern": "^[0-9A-Za-z][0-9A-Za-z._-]{0,95}\\Z",
          "description": "The TOOL INSTANCE attempt id."
        },
        "path": {
          "type": "string",
          "pattern": "^[A-Za-z0-9._+@%~,-][A-Za-z0-9._+@%~,/-]{0,1023}\\Z"
        },
        "sha256": {
          "type": "string",
          "pattern": "^[0-9a-f]{64}\\Z",
          "description": "Hash of the cited output FILE (bare hex, as in threat-model-citation). Never a hash of a matched value."
        }
      }
    }
  }
}
