{
  "schema": "appsec-review/output-contract/0.1",
  "contract_id": "critical-findings-sarif",
  "display_name": "Critical Findings SARIF",
  "required_files": [
    "manifest.json",
    "outputs/critical-findings.sarif",
    "status.json"
  ],
  "required_status_fields": [
    "status",
    "attempt_id",
    "dagster_run_id"
  ],
  "result_schema": {
    "artifact": "outputs/critical-findings.sarif",
    "schema_file": "critical-findings-sarif.schema.json"
  },
  "claim_types": [
    "critical_findings_sarif"
  ],
  "validation_rules": [
    "strict finding fields and unique IDs",
    "SARIF 2.1.0 envelope and result count",
    "the declared SARIF result validates against critical-findings-sarif.schema.json",
    "fresh run-owned input hash",
    "immutable output and manifest hashes",
    "common v1.0 worker-result envelope and read-only publication boundary",
    "argv-only appsec-review/deterministic-child/1.0 execution with separate bounded streams",
    "latest failed attempt blocks old success",
    "this contract republishes an upstream verification decision; it does not create, verify, or upgrade a finding"
  ]
}
