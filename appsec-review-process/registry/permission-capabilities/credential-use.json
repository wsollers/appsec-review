{
  "schema": "appsec-review/permission-capability/1.0",
  "capability_id": "credential-use",
  "kind": "credential-use",
  "version": "1.0",
  "display_name": "Credential use",
  "description": "Use one operator-provisioned credential, named by reference id, for one closed read-only scope.",
  "required_parameters": [
    "credential_ref",
    "credential_scope"
  ],
  "default_decision": "DENY",
  "allowed_requirement_origins": [
    "registry",
    "operator",
    "staged-run-config"
  ],
  "allowed_grant_origins": [
    "operator",
    "staged-run-config"
  ],
  "boundary_notes": [
    "Records carry the reference id only. The credential value is resolved by the runtime secret provider and must never appear in any permission record.",
    "Does not by itself authorize a network destination."
  ]
}
