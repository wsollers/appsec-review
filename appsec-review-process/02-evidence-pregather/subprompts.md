# Subprompts — Evidence Pregather

## Failure Triage

Given a degraded job, inspect `job-manifest.jsonl`, failing logs, and missing artifacts. Produce the
smallest safe rerun command.

## LLM Package Refresh

Regenerate `deep-confirmation` and `ENGAGEMENT_LLM_INPUT.md` from existing evidence without rerunning
the whole stack.

