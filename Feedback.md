[P1] Reconcile the accepted ADR with the contracts this PR publishes

The new registry contracts and validators deliberately implement a different authoritative surface from `docs/decisions/ADR-0010-vendor-prepass-decomposition.md`: the accepted ADR still names singular `vulnerability-database-identity.json` and no per-component gap/summary artifacts (line 358), still declares `02-license-scan` as depending only on `00-intake` (lines 315 and 323-329), and still says SCA reads the NVD snapshot (line 413). This PR instead requires plural Grype/OSV identities, `sca-coverage-gaps.json`, `coverage-gap-summary.json`, and an accepted SBOM input for license validation. The PR documentation acknowledges these conflicts but leaves both versions in-tree. Update the accepted ADR/topology and contract table in this PR (including the SBOM-to-license edge and current SCA data sources), or align the implementation to the accepted decision, so downstream integrators do not have two incompatible sources of truth.
