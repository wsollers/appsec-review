{
  "_comment": "Hand-maintained catalog entries for everything that is not a job-graph.json lifecycle node: operator scripts, human tasks, standalone Dagster jobs and their ops. consumes/produces name artifacts from artifacts.json, or job:<id> for an accepted lifecycle-job output. Keep in step with the BPMN model and flow-bringup.md; job_catalog.py --check fails when a reference is unknown.",
  "steps": [
    {"id": "populate-targets", "name": "Check out the system under test", "kind": "script",
     "runs": "fixtures/populate-targets.sh", "bpmn": ["sp1_clone", "sp1_origin", "sp1_status", "sp1_fetch", "sp1_checkout"],
     "consumes": ["sut-origin", "sut-pin"], "produces": ["sut-checkout"],
     "notes": "Clones or fetches, verifies origin, refuses local changes (never resets), checks out the pinned commit and verifies HEAD."},

    {"id": "docker-check", "name": "Check the Docker engine", "kind": "script",
     "runs": "docker version", "bpmn": ["sp2_docker"],
     "consumes": [], "produces": ["docker-engine"],
     "notes": "Docker Desktop must be the only engine: a native docker.service in the distro competes for /var/run/docker.sock (500 on /version, hangs). docker info reports OperatingSystem \"Docker Desktop\"; systemctl is-active docker reports inactive."},
    {"id": "docker-recover", "name": "Recover Docker Desktop", "kind": "human",
     "runs": "wsl --shutdown; restart Docker Desktop", "bpmn": ["sp2_recover"],
     "consumes": [], "produces": ["docker-engine"], "notes": "Only when docker-check fails. If a native docker service is active in the distro, disable it first (sudo systemctl disable --now docker.service docker.socket containerd.service): it was the root cause of the repeated wedges (2026-09-23)."},
    {"id": "compose-setup", "name": "Prepare compose settings and host storage", "kind": "script",
     "runs": "orchestrator/dagster/setup.py", "bpmn": ["sp2_setup"],
     "consumes": [], "produces": ["compose-env", "host-dirs"],
     "notes": "Writes the .env password and operator uid/gid; creates .host/ as the operator."},
    {"id": "compose-up", "name": "Start Dagster services", "kind": "script",
     "runs": "docker compose -f orchestrator/dagster/compose.yaml up -d; compose ps", "bpmn": ["sp2_up", "sp2_ps", "sp2_wait"],
     "consumes": ["docker-engine", "compose-env", "host-dirs"], "produces": ["dagster-services"],
     "notes": "Waits for webserver, daemon and postgres to report healthy."},
    {"id": "code-location-start", "name": "Start the host code location", "kind": "script",
     "runs": "orchestrator/dagster/code-location.sh start", "bpmn": ["sp2_cl", "sp2_recreate"],
     "consumes": ["job-definitions", "compose-env"], "produces": ["code-location-venv", "code-location", "compose-env"],
     "notes": "Builds the Python 3.12 venv, checks git and libfuzzy, runs dagster code-server start; if the WSL IP changed it updates .env and recreates webserver and daemon."},
    {"id": "code-location-check", "name": "Check the code location", "kind": "script",
     "runs": "orchestrator/dagster/code-location.sh check", "bpmn": ["sp2_check"],
     "consumes": ["code-location"], "produces": [], "notes": "gRPC health check from the host."},
    {"id": "code-location-reload", "name": "Reload job definitions", "kind": "script",
     "runs": "orchestrator/dagster/code-location.sh reload", "bpmn": ["sp2_reload"],
     "consumes": ["code-location", "dagster-services", "job-definitions"], "produces": ["loaded-job-list"],
     "notes": "GraphQL reloadRepositoryLocation, retried for 60 s; required after every start and after pulling job code."},
    {"id": "nop", "name": "nop (plumbing proof)", "kind": "dagster-job",
     "runs": "Dagster job nop", "bpmn": [],
     "consumes": ["loaded-job-list", "code-location"], "produces": [],
     "notes": "No run data, target or Docker; proves submit, queue and host execution."},

    {"id": "define-engagement", "name": "Define the engagement", "kind": "human",
     "runs": "security engineer", "bpmn": ["sp3_define"],
     "consumes": [], "produces": ["engagement-definition"],
     "notes": "Business goal, platforms, budget, scope and permissions."},
    {"id": "grant-permissions", "name": "Grant permissions beyond read-source", "kind": "human",
     "runs": "named approver", "bpmn": ["sp3_grant"],
     "consumes": ["engagement-definition"], "produces": ["permission-grant"],
     "notes": "Only when the engagement needs more than read-source."},
    {"id": "run-start", "name": "Create the run", "kind": "script",
     "runs": "appsec-review-process/run_process.py --start", "bpmn": ["sp3_create"],
     "consumes": [], "produces": ["run-dir"],
     "notes": "New run ID and run-owned folders; runs on the host (ADR-0011)."},
    {"id": "stage-artifacts", "name": "Stage the inputs", "kind": "script",
     "runs": "appsec-review-process/stage_artifacts.py --run-id <run_id> --target <host path> ...", "bpmn": ["sp3_stage"],
     "consumes": ["run-dir", "sut-checkout", "engagement-definition", "permission-grant"], "produces": ["artifact-manifest"],
     "notes": "Records executor platform posix and fingerprints inputs; refuses legacy runs, other platforms and unimported evidence."},

    {"id": "phase1_intake", "name": "phase1_intake (Dagster job)", "kind": "dagster-job",
     "runs": "launch_job.py --run-id <run_id> --job phase1_intake --wait", "bpmn": ["sp4_intake"], "lifecycle": ["00-intake"],
     "consumes": ["artifact-manifest", "loaded-job-list"], "produces": ["resolved-config", "job:00-intake"],
     "notes": "Ops intake_config, intake_pre_validation, intake_work, intake_post_validation; in-process executor, no retries."},
    {"id": "intake-fix", "name": "Correct inputs after an intake failure", "kind": "human",
     "runs": "security engineer", "bpmn": ["sp4_intfix"],
     "consumes": ["resolved-config"], "produces": ["artifact-manifest"], "notes": "Inspect, correct, restage."},
    {"id": "repository_partition_discovery", "name": "repository_partition_discovery (Dagster job)", "kind": "dagster-job",
     "runs": "launch_job.py --run-id <run_id> --job repository_partition_discovery --wait", "bpmn": ["sp4_pgate"], "lifecycle": ["02-repository-partition-discovery"],
     "consumes": ["job:00-intake", "partition-supplied", "loaded-job-list"], "produces": ["job:02-repository-partition-discovery", "partition-handoff"],
     "notes": "Supplied-result gate: accepts a valid, fresh map or fails with a hand-off; never invents the analysis."},
    {"id": "partition-analysis", "name": "Write the partition map", "kind": "human",
     "runs": "analyst or agent (fixtures: tracked record)", "bpmn": ["sp4_panalyze"],
     "consumes": ["partition-handoff", "sut-checkout"], "produces": ["partition-record"],
     "notes": "Partitions, kind of code, persona routing, relationships, scope; SHA-256 citations."},
    {"id": "supply-partition", "name": "Install the supplied partition map", "kind": "script",
     "runs": "fixtures/supply_record.py --run-id <run_id> --job 02-repository-partition-discovery", "bpmn": ["sp4_psupply"],
     "consumes": ["partition-record", "sut-checkout", "run-dir"], "produces": ["partition-supplied"],
     "notes": "Refuses a target not at the record's commit or with local changes; never overwrites a different supplied file."},
    {"id": "dev_project_discovery", "name": "dev_project_discovery (Dagster job)", "kind": "dagster-job",
     "runs": "launch_job.py --run-id <run_id> --job dev_project_discovery --wait", "bpmn": ["sp4_dgate"], "lifecycle": ["02-dev-project-discovery"],
     "consumes": ["job:02-repository-partition-discovery", "dev-supplied", "loaded-job-list"], "produces": ["job:02-dev-project-discovery", "dev-handoff"],
     "notes": "Same gate pattern; also requires the accepted partition map at the same source revision."},
    {"id": "dev-analysis", "name": "Write the project discovery", "kind": "human",
     "runs": "analyst or agent (fixtures: tracked record)", "bpmn": ["sp4_danalyze"],
     "consumes": ["dev-handoff", "sut-checkout", "job:02-repository-partition-discovery"], "produces": ["dev-record"],
     "notes": "Project root, manifests, build image, safe command plan."},
    {"id": "supply-dev", "name": "Install the supplied project discovery", "kind": "script",
     "runs": "fixtures/supply_record.py --run-id <run_id> --job 02-dev-project-discovery", "bpmn": ["sp4_dsupply"],
     "consumes": ["dev-record", "sut-checkout", "run-dir"], "produces": ["dev-supplied"], "notes": "Same checks as supply-partition."},

    {"id": "launch-job", "name": "Submit a job to Dagster", "kind": "script",
     "runs": "appsec-review-process/launch_job.py --run-id <run_id> --job <job> [--wait]", "bpmn": ["sp5_manifest", "sp5_request", "sp5_find", "sp5_intent", "sp5_launch"],
     "consumes": ["artifact-manifest", "loaded-job-list"], "produces": ["launch-request", "launch-intent", "dagster-run-queued"],
     "notes": "Allow-listed jobs only; refuses non-POSIX runs; records SUBMITTING before launchRun; finds an existing run by request and engagement tags instead of launching twice."},
    {"id": "dagster-accept", "name": "Dagster accepts or rejects the launch", "kind": "dagster-service",
     "runs": "Dagster webserver and daemon", "bpmn": ["D_queue", "D_err", "D_respond"],
     "consumes": ["launch-intent"], "produces": ["dagster-run-queued"],
     "notes": "Queue limits: 2 runs overall, 1 per engagement. Validation errors come back as REJECTED."},

    {"id": "engagement_workflow", "name": "engagement_workflow (Dagster job)", "kind": "dagster-job",
     "runs": "launch_job.py --run-id <run_id> --job engagement_workflow --wait", "bpmn": [], "lifecycle": ["00-intake"],
     "consumes": ["artifact-manifest", "loaded-job-list"], "produces": ["job:00-intake", "prep-scope", "prep-native-plan", "prep-handoffs", "workflow-status"],
     "notes": "Preparation workflow: config, intake, three preparation branches, validated join. Not a full lifecycle review."},
    {"id": "op-workflow_config", "name": "workflow_config (op)", "kind": "dagster-op", "runs": "engagement_workflow", "bpmn": [],
     "consumes": ["artifact-manifest"], "produces": ["workflow-status"],
     "notes": "Checks the engagement_run_id tag, takes generation ownership, records the attempt config."},
    {"id": "op-workflow_intake", "name": "workflow_intake (op)", "kind": "dagster-op", "runs": "engagement_workflow", "bpmn": [],
     "consumes": ["artifact-manifest", "workflow-status"], "produces": ["job:00-intake"],
     "notes": "Atomic intake: configure, prepare, work (or reuse), publish."},
    {"id": "op-scope_check", "name": "scope_check (op)", "kind": "dagster-op", "runs": "engagement_workflow", "bpmn": [],
     "consumes": ["job:00-intake"], "produces": ["prep-scope"], "notes": "Preparation branch."},
    {"id": "op-native_plan_check", "name": "native_plan_check (op)", "kind": "dagster-op", "runs": "engagement_workflow", "bpmn": [],
     "consumes": ["job:00-intake"], "produces": ["prep-native-plan"], "notes": "Preparation branch."},
    {"id": "op-discovery_handoffs", "name": "discovery_handoffs (op)", "kind": "dagster-op", "runs": "engagement_workflow", "bpmn": [],
     "consumes": ["job:00-intake"], "produces": ["prep-handoffs"], "notes": "Preparation branch."},
    {"id": "op-workflow_publish", "name": "workflow_publish (op)", "kind": "dagster-op", "runs": "engagement_workflow", "bpmn": [],
     "consumes": ["job:00-intake", "prep-scope", "prep-native-plan", "prep-handoffs"], "produces": ["workflow-status"],
     "notes": "Rechecks freshness, validates every branch, publishes the join."},

    {"id": "build_discovery", "name": "build_discovery (Dagster job)", "kind": "dagster-job",
     "runs": "launch_job.py --run-id <run_id> --job build_discovery --wait", "bpmn": [],
     "consumes": ["artifact-manifest", "job:00-intake"], "produces": ["build-discovery", "workflow-status"],
     "notes": "Cited build requirements and command arrays; nothing is executed."},
    {"id": "build_execution", "name": "build_execution (Dagster job)", "kind": "dagster-job",
     "runs": "launch_job.py --run-id <run_id> --job build_execution --wait", "bpmn": [], "lifecycle": ["02-build-configure"],
     "consumes": ["job:00-intake", "build-discovery", "buildenv-image"], "produces": ["build-execution"],
     "notes": "Standalone diagnostic: one CMake root only, via the buildenv-common wrapper, not B13. Superseded for S6 by batch E01."},
    {"id": "evidence_index", "name": "evidence_index (Dagster job)", "kind": "dagster-job",
     "runs": "launch_job.py --run-id <run_id> --job evidence_index --wait", "bpmn": [], "lifecycle": ["02-evidence-index"],
     "consumes": ["job:00-intake", "build-discovery"], "produces": ["job:02-evidence-index"],
     "notes": "Independent source branch; the index is a locator, never evidence authority."},
    {"id": "critical_findings_sarif", "name": "critical_findings_sarif (Dagster job)", "kind": "dagster-job",
     "runs": "launch_job.py --run-id <run_id> --job critical_findings_sarif --wait", "bpmn": [], "lifecycle": ["10-synthesis-report"],
     "consumes": ["critical-findings-md"], "produces": ["job:10-synthesis-report"],
     "notes": "Fixed-input transform to accepted SARIF; the final lifecycle publication binding is missing."},
    {"id": "ossf_scorecard", "name": "ossf_scorecard (Dagster job)", "kind": "dagster-job",
     "runs": "launch_job.py --run-id <run_id> --job ossf_scorecard --wait", "bpmn": [], "lifecycle": ["02-ossf-scorecard"],
     "consumes": ["job:00-intake"], "produces": ["job:02-ossf-scorecard"], "notes": "Published-results ingestion (network pool)."},
    {"id": "nvd_reference_sync", "name": "nvd_reference_sync (Dagster job)", "kind": "dagster-job",
     "runs": "schedule nvd_reference_schedule (every 2 h, UTC) or manual launch", "bpmn": [],
     "consumes": ["nvd-api"], "produces": ["nvd-snapshot"],
     "notes": "Shared reference feed outside every engagement run; APPSEC_NVD_SCHEDULE=stopped disables the schedule."},
    {"id": "orchestration_smoke", "name": "orchestration_smoke (Dagster job)", "kind": "dagster-job",
     "runs": "Dagster job orchestration_smoke", "bpmn": [],
     "consumes": ["loaded-job-list"], "produces": [], "notes": "Pre-validation, work, post-validation smoke path."},
    {"id": "full_review", "name": "full_review (Dagster job)", "kind": "dagster-job",
     "runs": "launch_job.py --run-id <run_id> --job full_review --wait", "bpmn": [], "lifecycle": ["*"],
     "consumes": ["artifact-manifest", "loaded-job-list"], "produces": ["job:10-synthesis-report"],
     "notes": "The whole lifecycle graph (51 nodes); stops at the first node with no worker. Its per-node inputs and outputs are in Appendix B."},

    {"id": "legacy-pregather", "name": "Legacy evidence pipeline", "kind": "script",
     "runs": "pipeline/engagement_job.sh / .ps1 (outside Dagster)", "bpmn": [],
     "consumes": ["sut-checkout"], "produces": ["legacy-pregather"],
     "notes": "Static prepass, native pregather, assemble, correlate, deep_confirm, retrieval plan; imported into a run by hashed import. Being replaced by the 02-* graph nodes."},
    {"id": "lane-handoffs", "name": "Dispatch LLM lanes", "kind": "script",
     "runs": "appsec-review-process/create_handoff.py", "bpmn": [],
     "consumes": ["job:02-evidence-index", "legacy-pregather"], "produces": ["lane-handoff"],
     "notes": "Hand-off files for the tracked prompt harness; lanes are not yet graph workers."}
  ]
}
